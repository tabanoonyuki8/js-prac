const str = "Education is fun!".toLowerCase();

if (str.includes('a') && str.includes('e') && 
str.includes('i') &&
str.includes('o') &&
str.includes('u')) {
  console.log("The string contains all the vowels.");
} else {
  console.log("The string does NOT contain all the vowels.");
}
